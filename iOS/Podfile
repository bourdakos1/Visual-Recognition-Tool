# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'Visual Recognition' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Visual Recognition
  source 'https://github.com/CocoaPods/Specs.git'
  pod 'Zip', '~> 1.1'
  pod 'Alamofire', '~> 4.7'

  target 'Visual RecognitionTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'Visual RecognitionUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
